make_roster <- function(norgs, nlevels=4, nplayers=25) {
  # Loop over orgs
  for (i in 1:norgs) {
    # Loop over levels
    for (j in 1:nlevels) {
      for (k in 1:nplayers) {
        # Check csv to see if player was created
        # Make player
        # Write to csv that player was created
      } # end player
      # Navigate to FA page, clear team, then add them to team
      # Write that team was created
    } # end level
  } # end org
}
